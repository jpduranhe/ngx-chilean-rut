name: Publish to NPM

on:
  release:
    types: [created]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: self-hosted
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # Gate de calidad: si el lint o los tests fallan, NO se publica.
      # Sin esto, una suite roja llega a npm sin que nadie se entere.
      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test:coverage

      - name: Build Library
        run: npm run build

      - name: Authenticate with NPM
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN || secrets.NPM_SECRET_TOKEN }}" > .npmrc

      - name: Publish
        run: npm publish --access public
        working-directory: dist/ngx-chilean-rut
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.NPM_SECRET_TOKEN }}
          NPM_CONFIG_USERCONFIG: ${{ github.workspace }}/.npmrc
